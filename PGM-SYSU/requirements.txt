# PGM-SYSU 实验环境依赖
# 生成时间: 2026-04-29
# Python: 3.8.10
# CUDA: 11.3 (nvcc 11.3)
# GPU: NVIDIA GeForce RTX 3090
#
# 注意：torch 和 torchvision 建议通过以下命令安装（cu113 版本）：
#   pip install torch==1.11.0+cu113 torchvision==0.12.0+cu113 \
#       --extra-index-url https://download.pytorch.org/whl/cu113
#
# faiss 可根据环境选择 cpu 或 gpu 版本：
#   faiss-cpu==1.7.2    # 纯 CPU
#   faiss-gpu>=1.7.0    # 有 GPU 时推荐

# --- 核心深度学习框架 ---
torch==1.11.0+cu113
torchvision==0.12.0+cu113

# --- 图像处理与数值计算 ---
numpy==1.22.4
Pillow==9.1.1
scipy==1.10.1

# --- 机器学习 ---
scikit-learn==1.3.2

# --- 向量检索 (re-ranking 用) ---
faiss-cpu==1.7.2

# --- 可视化与日志 ---
matplotlib==3.5.2
tensorboard==2.9.1
tqdm==4.61.2

# --- CLIP 模型相关 (可选，如不使用 CLIP 可忽略) ---
# ftfy
# regex
